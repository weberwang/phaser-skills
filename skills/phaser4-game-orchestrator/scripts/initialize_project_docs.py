#!/usr/bin/env python3
"""按阶段初始化 Phaser 4 游戏项目的中文协作文档。"""

from __future__ import annotations

import argparse
import sys
from pathlib import Path


CORE_TEMPLATES = {
    "project-profile.yaml": """# 项目配置是角色协作的机器可读事实来源。
project:
  名称: 待定
  当前阶段: 待总控根据项目现状确定
  版本: 0.1.0
workflow:
  当前通道: 待总控根据任务影响确定
  升级条件: 跨模块、架构或数据边界、成套视觉重做、正式资源替换、参考还原、资源权属、目标渠道、质量或发布风险发生变化
channels:
  首发渠道: 待人工指定
  候选渠道: []
capabilities:
  # 可选能力默认关闭；只有实际启用时才需要单独评审实现、数据和合规影响。
  登录: false
  云存档: false
  内购: false
  广告: false
  埋点: false
  推送: false
  排行榜: false
  联机: false
quality_targets:
  当前档位: 原型
  原型验收: 干净环境可构建且核心流程可完成
  发布指标: 待进入发布通道时按目标渠道定义
""",
    "GDD.md": """# 游戏设计文档

## 一句话定义

待人工定义。

## 玩家与核心循环

待人工定义：目标玩家、操作方式、目标、反馈、失败与重试。

## 最小可交付范围

待人工批准。

## 功能验收条件

| 功能 | 玩家行为 | 可观察结果 | 负责人 | 状态 |
| --- | --- | --- | --- | --- |

## 参考功能还原契约

仅在参考截图、录屏、运行项目或源代码还原任务中使用。静态截图无法证明的行为标为“待人工定义”，不得自行推断。

| 契约 ID | 场景/对象 | 前置状态 | 输入/操作轨迹 | 规则与状态迁移 | 时序与反馈 | 成功/失败/异常结果 | 参考证据 | 验收方式 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 明确排除项

待人工定义。
""",
    "visual-design.md": """# 全局视觉设计

本文档是角色、场景、UI、特效、图标与商店视觉的统一基线。未获人工批准的内容标为待定，不得作为批量生成或视觉还原依据。

## 视觉方向

- 体验目标：待人工定义。
- 风格关键词：待人工定义。
- 参考图及允许借鉴范围：待补充来源、版本、权属和用途。
- 禁用方向：待人工定义。

## 视觉系统

| 范畴 | 已冻结规则 | 可变量 | 禁止项 | 验收方式 |
| --- | --- | --- | --- | --- |
| 色彩与对比 | 待定 | 待定 | 待定 | 待定 |
| 字体与排版 | 待定 | 待定 | 待定 | 待定 |
| 角色与道具 | 待定 | 待定 | 待定 | 待定 |
| 场景与光影 | 待定 | 待定 | 待定 | 待定 |
| UI 与图标 | 待定 | 待定 | 待定 | 待定 |
| 动画与特效 | 待定 | 待定 | 待定 | 待定 |

## 技术约束

待补充基准分辨率、适配策略、像素密度、安全区、缩放/采样方式、图集与纹理预算。

## 无交互满幅背景

背景不得承载交互、文字、关键目标或玩法信息。先生成满足“竖屏高度适配、横屏宽度适配”覆盖约束的方向版本并完成人工确认，再通过统一适配器应用到所有可见场景。

| 背景 ID | 方向 | 适用场景 | 资源/程序化方案 | 基准尺寸 | 高度/宽度适配 | 焦点安全区 | 裁切/延展规则 | 人工确认 | 全场景验证 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 代表性基准画面

| 基准画面 | 目标场景/状态 | 来源与版本 | 固定视口/设备 | 审查状态 | 证据 |
| --- | --- | --- | --- | --- | --- |

## 视觉与功能还原清单

| 对象/稳定 ID | 输入类型 | 输入位置/代码入口 | 功能契约 ID | 当前问题 | 代码绘制/复用/新生成/重绘/删除 | 目标规格 | 生成版本 | 人工确认/修改意见 | 功能证据 | 视觉对比证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 一致性验收

- [ ] 垂直切片中的角色、场景、UI、图标和特效符合已冻结规则。
- [ ] 关键状态在固定视口下具有可复现的重构前后截图。
- [ ] 参考还原任务的输入、规则、状态迁移、时序、反馈、成功/失败和异常路径均有契约与复现证据。
- [ ] 功能验收与逐状态视觉对比均通过，未使用一侧结果替代另一侧。
- [ ] 每个生成或重绘产物均有明确的人工确认记录，要求修改的版本已经重新确认。
- [ ] A1、A2、A4、A5、A6 的确认分别指向当前阶段产物；参考输入、沉默或其他阶段批准未被推断为通过。
- [ ] 参考图与运行图使用相同视口宽高、设备像素比、游戏状态、操作轨迹、文本语言和动画时间点，并保存并排、叠加或像素差异图。
- [ ] 源码标记测试仅标为结构回归，没有被用作视觉通过证据。
- [ ] 每个可见场景均接入已确认的无交互背景，且背景不承载交互或玩法信息。
- [ ] 竖屏背景按高度缩放，横屏背景按宽度缩放，另一轴仅执行批准的裁切或延展。
- [ ] 目标最窄/最宽视口、方向/尺寸变化和场景过渡期间四边四角无黑边、透明缝或闪烁。
- [ ] 信息层级、可读性、触控安全区和反馈辨识度通过实机检查。
- [ ] 所有正式视觉资源均已登记来源、生成记录、授权和 Phaser 验证。
""",
    "TDD.md": """# 技术设计文档

## 技术基线

- 游戏：Phaser 4 + TypeScript + Vite
- 原生容器：Capacitor
- 渠道适配：小游戏、iOS、Google Play 分别评估

## 模块边界

待技术架构角色补充：场景、状态、输入、资源、存档、平台与可选商业服务。

## 模块划分与批准

新建模块、首次实现模块或改变职责、公开契约、状态所有权、依赖方向前，必须先形成候选模块图，由总控执行 `$grilling` 并获人工批准。状态使用：待拷问、已批准待实现、已实现、待复核、废弃。

| 模块 | 职责与非目标 | 公开输入/输出 | 状态/数据所有权 | 允许/禁止依赖 | 生命周期与失败边界 | 测试与验收 | 实现顺序 | 拷问结论/证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 模块依赖矩阵

| 调用模块 | 被调用模块 | 公开契约 | 调用方向理由 | 契约测试 | 批准状态 |
| --- | --- | --- | --- | --- | --- |

## 游戏实现增量

| 增量 | 玩家行为与验收 | 代码入口 | 资源依赖 | 测试 | 状态 |
| --- | --- | --- | --- | --- | --- |

## 参考功能契约实现映射

| 契约 ID | 模块/公开入口 | 状态所有者 | 实现路径 | 契约测试/集成测试 | 运行录屏 | 与参考差异 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |

## 资源接入契约

| 用途/状态 | 低保真 ID/路径 | 单图 ID | 正式路径 | 纹理键/帧名 | 锚点/九宫格 | A1-A6 状态 | 正式装配 | 低保真清理 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 全场景背景适配

| 场景 | 竖屏背景 ID | 横屏背景 ID | 适配器入口 | 竖高/横宽规则 | 焦点/裁切规则 | 最低层级且无交互 | 方向/尺寸变化 | 黑边验证 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 构建与运行

待技术架构角色记录可复现命令、版本和环境要求。

## 本地验证服务

启动前按项目根目录、服务类型、模式、端口、监听进程和健康状态查重。身份一致且健康时复用；只有不存在可复用实例时才新启动。

| 服务 | 项目根目录 | 模式/命令 | 主机/端口 | PID/会话 | 健康检查 | 复用/新启动 | 启动者 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 风险与待决策

待补充。
""",
    "control-plane.md": """# 项目控制面

本文件由总控独占写入。标准、发布通道记录当前状态和索引；快速通道仅记录未决决策或阻断项。各角色向总控提交状态包，不直接修改本文件。规则、实现细节与完整证据写入对应交付物。质量门汇合后删除已无后续影响的条目。

## 当前质量门

| 阶段 | 状态 | 通过条件或阻断项 | 最近证据 |
| --- | --- | --- | --- |
| 待总控确定 | 待人工决策 | 待总控根据项目现状选择质量门 |  |

## 待人工决策与拷问记录

| 日期 | 阶段 | 触发原因 | 已核实事实 | 问题与推荐项 | 人工结论 | 影响与下一步 |
| --- | --- | --- | --- | --- | --- | --- |

## 已确认变更

| 日期 | 变更 | 影响范围 | 复核质量门 | 关联证据 |
| --- | --- | --- | --- | --- |

## 角色交接

| 角色 | 当前输出或范围 | 状态 | 证据或阻断项 | 下游 |
| --- | --- | --- | --- | --- |

## 视觉完成审计

视觉任务汇报完成前逐项填写；任何证据为空时不得报告高保真完成、视觉还原通过或 A6 完成。

| 审计问题 | 结论 | 证据位置 |
| --- | --- | --- |
| 为什么不能或可以进入快速通道？ | 待审计 |  |
| A2、A4、A5 的当前版本人工确认在哪里？ | 待审计 |  |
| 哪些元素使用正式资源，哪些使用代码绘制？ | 待审计 |  |
| 代码绘制对象为什么符合允许范围？ | 待审计 |  |
| 参考图和运行图是否使用相同视口等条件？ | 待审计 |  |
| 视觉测试验证的是结构还是最终画面？ | 待审计 |  |
| 是否仍有低保真或占位实现？ | 待审计 |  |
""",
}

OPTIONAL_TEMPLATES = {
    "balance": (
        "balance.md",
        """# 数值设计

## 设计目标与玩家节奏

待数值设计角色补充。

## 参数与公式

| 参数 | 含义 | 默认值 | 来源 | 调整影响 | 验证方式 |
| --- | --- | --- | --- | --- | --- |

## 模拟与实测记录

待补充。
""",
    ),
    "assets": (
        "asset-license-register.md",
        """# 资源与授权登记

本文件由美术接入角色独占写入。音频角色提交资源登记包，由美术接入角色串行登记，避免共享文件并发修改。

| 资源 | 资源 ID | 输入/来源版本 | 处理状态 | 类型 | 用途 | 来源 | 授权/生成记录 | 人工确认/修改意见 | 规格/锚点/焦点 | 交付路径/纹理键 | Phaser 验证 | 发布可用 | 优化状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 无交互满幅背景验收

先于其他场景美术生成背景并完成人工确认。竖屏版本按高度缩放，横屏版本按宽度缩放；记录中心焦点安全区、四周裁切/延展区、目标最窄/最宽宽高比、纹理预算与统一适配器入口。逐个可见场景验证最低层级、无交互、方向/尺寸变化和切换过程，四边四角不得出现黑边、透明缝、闪烁或未批准纯色带。

## 视觉与功能还原验收

截图、录屏或运行输入记录原始位置、内容版本、视口、设备像素比、游戏状态、操作轨迹、文本语言、动画时间点和权属；代码输入记录场景/组件/规则入口、复现命令和提交版本，并先运行得到固定视口基准截图与功能轨迹。按对象稳定 ID 关联 GDD/TDD 中的参考功能契约，记录语义、层级、输入、状态迁移、反馈与“代码绘制、复用、新生成、重绘、删除”结论。代表性样张审查通过后才可批量生成；每轮生成或重绘后记录版本、预览、人工确认或修改意见，明确确认后才能拆分和接入。完成后在全部条件一致时保存参考图、运行图及并排、叠加或像素差异图，以及功能契约和 Phaser 运行证据。

生成记录至少包含使用方式、提示词或人工编辑摘要、模型/工具、日期、版本与人工审查结论。源代码分析不能替代运行截图，单张静态画面不能替代关键状态与目标设备验证。

## 美术生产与单图验收

| 目标预制体/场景 | 低保真路径与版本 | A1 确认 | 高保真效果图与版本 | A2 确认 | A3 审阅 | A4 框选图/单图清单 | A4 确认 | A5 单图验收 | A5 确认 | 正式装配证据 | 低保真清理证据 | A6 运行验收 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

A4 确认后，优先按来源内容哈希与框选编号生成唯一拆分 ID；无法计算哈希时才使用规范化来源路径、明确版本与框选编号。查询上表并复用文件存在、纹理键可寻址且验证仍有效的正式产物。逐项记录用途、尺寸、透明要求、锚点、九宫格、纹理键及“代码绘制、复用、新生成、重绘、删除”结论。框选图、清单、来源版本或规格变化后必须返回 A4 重新确认。

单图生成后逐项记录实际文件、来源和登记、透明边缘与完整性检查、深浅底对比预览、Phaser 实际加载与目标缩放截图、目标视口运行证据及当前版本 A5 确认；同时维护未完成/未验收资源清单，即使为空也明确记录。旧版本标为“已取代”，不得静默覆盖。A6 由玩法角色独占执行正式结构装配和低保真实现清理；美术接入角色审阅并登记其测试和构建证据。任一确认门、正式装配或低保真清理未通过时不得标为可交付。

## 替换计划

临时资源必须标注替换负责人和截止质量门。
""",
    ),
    "qa": (
        "qa-plan.md",
        """# 测试与性能计划

## 缺陷分级

- P0：无法启动、数据丢失、合规阻断或核心流程不可完成。
- P1：关键体验或目标渠道功能不可接受。
- P2：可绕过的功能或体验问题。
- P3：非阻断的优化项。

## 模块边界验证

| 模块 | 实现前拷问/批准证据 | 职责与状态所有权一致 | 公开契约测试 | 禁止依赖检查 | 结果 |
| --- | --- | --- | --- | --- | --- |

## 本地服务复用记录

| 项目/服务/模式 | 启动前监听与进程检查 | 健康检查 | 复用/查重后新启动 | URL/端口/PID | 重复实例检查 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |

## 设备与性能矩阵

目标设备和指标待人工指定；未指定前不得判定性能达标。

| 渠道/设备 | 版本 | 测试项 | 帧率 | 启动时间 | 内存/包体 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |

## 全场景背景覆盖

| 场景 | 方向/宽高比 | 高度/宽度适配正确 | 创建/切换/缩放状态 | 四边四角无黑边 | 无透明缝/闪烁 | 不截获输入 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |

## 参考视觉与功能双验收

| 契约 ID/状态 | 游戏状态与操作轨迹 | 文本语言/动画时间点 | 功能预期/实际 | 功能结果 | 参考图/运行图 | 并排/叠加/像素差异图 | 视觉结果 | 视口宽高/设备像素比 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

参考图与运行图任一对比条件不一致时，视觉结果只能标记“未验证”。源码方法名、固定坐标、类型检查、单元测试、生产构建、控制台无错误或元素存在只能用于结构与工程验证；源码标记测试必须命名为“结构回归”，不得表述为“视觉回归”。
""",
    ),
    "platform": (
        "platform-matrix.md",
        """# 平台矩阵

| 渠道 | 优先级 | 打包方式 | 必要适配 | 商店/平台资料 | 合规状态 | 负责人 |
| --- | --- | --- | --- | --- | --- | --- |
| 小游戏 | 待人工指定 | 待评估 | 待评估 | 待评估 | 待评估 | 发布合规 |
| iOS | 待人工指定 | Capacitor | 待评估 | 待评估 | 待评估 | 发布合规 |
| Google Play | 待人工指定 | Capacitor | 待评估 | 待评估 | 待评估 | 发布合规 |
| 其他 | 扩展 | 待评估 | 待评估 | 待评估 | 待评估 | 发布合规 |
""",
    ),
    "release": (
        "release-checklist.md",
        """# 发布检查清单

## 发布候选版本

- 版本号：
- 构建提交：
- 构建命令：
- 候选包位置：

## 必须核验

- [ ] 人工已批准 G3。
- [ ] 每个目标渠道都有对应候选包和测试证据。
- [ ] 商店资料、隐私声明、分级和资源授权已核验。
- [ ] 没有密钥或商店凭据进入仓库。
- [ ] 已知风险、回滚或热修复方案已记录。
""",
    ),
}


def parse_include_list(value: str) -> list[str]:
    """解析可选交付物名称，拒绝拼写错误以避免创建错误文档。"""
    names = [name.strip() for name in value.split(",") if name.strip()]
    unknown = sorted(set(names) - OPTIONAL_TEMPLATES.keys())
    if unknown:
        valid = "、".join(OPTIONAL_TEMPLATES)
        raise argparse.ArgumentTypeError(
            f"不支持的交付物：{'、'.join(unknown)}。可选值：{valid}。"
        )
    return list(dict.fromkeys(names))


def parse_args() -> argparse.Namespace:
    """解析显式项目目录、可选交付物与覆盖授权。"""
    parser = argparse.ArgumentParser(description="按阶段初始化 Phaser 4 游戏协作文档")
    parser.add_argument("--project-root", required=True, type=Path, help="目标游戏项目根目录")
    parser.add_argument(
        "--include",
        type=parse_include_list,
        help="逗号分隔的可选交付物：balance、assets、qa、platform、release",
    )
    parser.add_argument("--force", action="store_true", help="明确覆盖本次选择的同名文档")
    return parser.parse_args()


def validate_project_root(project_root: Path) -> Path:
    """拒绝文件系统根目录，降低误写入过宽目录的风险。"""
    resolved = project_root.resolve()
    if resolved == resolved.parent:
        raise ValueError("项目目录不能是文件系统根目录。")
    return resolved


def select_templates(include: list[str] | None) -> dict[str, str]:
    """默认选择核心交付物，指定 include 时只创建对应的阶段性交付物。"""
    if include is None:
        return CORE_TEMPLATES
    return {OPTIONAL_TEMPLATES[name][0]: OPTIONAL_TEMPLATES[name][1] for name in include}


def initialize_documents(
    project_root: Path, templates: dict[str, str], force: bool
) -> list[Path]:
    """仅创建或显式覆盖本次选择的交付物，避免影响其他阶段文档。"""
    docs_dir = project_root / "docs"
    targets = [docs_dir / filename for filename in templates]
    existing = [path for path in targets if path.exists()]
    if existing and not force:
        names = "、".join(path.name for path in existing)
        raise FileExistsError(f"拒绝覆盖已有文档：{names}。如确需覆盖，请显式传入 --force。")

    docs_dir.mkdir(parents=True, exist_ok=True)
    for path in targets:
        path.write_text(templates[path.name], encoding="utf-8")
    return targets


def main() -> int:
    """执行阶段初始化并输出可审计的写入结果。"""
    args = parse_args()
    try:
        project_root = validate_project_root(args.project_root)
        templates = select_templates(args.include)
        written = initialize_documents(project_root, templates, args.force)
    except (OSError, ValueError) as error:
        print(f"初始化失败：{error}", file=sys.stderr)
        return 1

    print("已初始化项目交接物：")
    for path in written:
        print(path)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
