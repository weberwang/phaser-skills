#!/usr/bin/env python3
"""按阶段初始化 Phaser 4 游戏项目的中文协作文档。"""

from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path


CORE_TEMPLATES = {
    "project-profile.yaml": """# 项目配置是角色协作的机器可读事实来源。
project:
  名称: 待定
  当前阶段: 待总控根据项目现状确定
  版本: 0.1.0
workflow:
  当前通道: 待总控根据任务影响确定
  当前审核层: F0入口筛选
  审核机制: F0入口筛选 → F1证据筛选 → F2非作者专业筛选 → F3综合筛选 → 按需F4人工决策
  视觉分流: V0原子资源/组件或资源集/场景或视觉系统或参考还原
  原子资源条件: 仅当已有适用且有效的玩法视觉契约与预算基线可引用时走V3至V5，否则升级路径
  升级条件: 跨模块、架构或数据边界、正式资源、视觉系统、参考还原、资源权属、目标渠道、质量或发布风险发生变化
channels:
  首发渠道: 待人工指定
  候选渠道: []
capabilities:
  # 可选能力默认关闭；只有实际启用时才需要单独评审实现、数据和合规影响。
  登录: false
  云存档: false
  内购: false
  广告: false
  埋点: false
  推送: false
  排行榜: false
  联机: false
quality_targets:
  当前档位: 原型
  原型验收: 干净环境可构建且核心流程可完成
  发布指标: 待进入发布通道时按目标渠道定义
""",
    "GDD.md": """# 游戏设计文档

## 一句话定义

待人工定义。

## 玩家与核心循环

待人工定义：目标玩家、操作方式、目标、反馈、失败与重试。

## 最小可交付范围

待人工批准。

## 功能验收条件

| 功能 | 玩家行为 | 可观察结果 | 负责人 | 状态 |
| --- | --- | --- | --- | --- |

## 参考功能还原契约

仅在参考截图、录屏、运行项目或源代码还原任务中使用。静态截图无法证明的行为标为“待人工定义”，不得自行推断。

| 契约 ID | 场景/对象 | 前置状态 | 输入/操作轨迹 | 规则与状态迁移 | 时序与反馈 | 成功/失败/异常结果 | 参考证据 | 验收方式 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 明确排除项

待人工定义。
""",
    "visual-design.md": """# 全局视觉设计

本文档统一角色、场景、UI、动画和特效的视觉基线。未冻结内容不得作为批量生产依据。

## 视觉方向与探索记录

- 体验目标：待定。
- 风格关键词与禁用方向：待定。
- 参考图、允许借鉴范围、来源与权属：待定。

| 方向候选 | 关键画面/动态样片 | 保留理由 | 淘汰理由 | 决策记录 |
| --- | --- | --- | --- | --- |

## 视觉系统

| 范畴 | 已冻结规则 | 可变量 | 禁止项 | 验收方式 |
| --- | --- | --- | --- | --- |
| 形状语言与轮廓 | 待定 | 待定 | 待定 | 待定 |
| 明度层级与色彩 | 待定 | 待定 | 待定 | 灰阶/色觉差异/小屏检查 |
| 材质、光照与空间 | 待定 | 待定 | 待定 | 待定 |
| 字体、排版与图标语法 | 待定 | 待定 | 待定 | 待定 |
| 角色比例与动作语言 | 待定 | 待定 | 待定 | 待定 |
| 动画时序与采样 | 待定 | 待定 | 待定 | 动态稳定帧/关键时间点 |
| VFX 层级与遮挡规则 | 待定 | 待定 | 待定 | 动态玩法片段 |

## 玩法视觉契约

逐状态定义第一视觉目标，并按玩家、目标、危险、交互、装饰排序。静态效果图、像素接近或资源齐全不能证明游戏性。

| 状态/契约 ID | 第一视觉目标 | 优先级 | 明度/色彩/轮廓/运动区分 | 可供性（affordance） | 前摇/命中/受击/奖励时序 | 遮挡预算 | 小屏/高速/灰阶/色觉阈值 | 动态可玩证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 早期技术预算

适用 V1 时定义场景级上限；原子资源引用既有适用且有效的预算基线。V3 写入机器清单，V4 只验证，不临时发明预算。

| 场景 | 纹理/图集 | 帧数与动画采样 | 纹理内存 | 包体 | 最大纹理 | 过绘 | Draw Call | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

## UI 精准布局基线

组件相对屏幕、安全区、父容器、具名兄弟或布局组定位；资源锚点与布局停靠点分别记录。

| 项目 | 已冻结规则 | 量化阈值 | 验收证据 |
| --- | --- | --- | --- |
| 基准视口、逻辑单位与安全区 | 待定 | 待定 | 待定 |
| 缩放、断点、方向与换行 | 待定 | 待定 | 待定 |
| 位置、尺寸、间距与累计漂移 | 待定 | 待定 | 待定 |

## 装饰性满幅背景

仅登记不承载交互、目标、碰撞或玩法信息的屏幕空间背景。世界空间关卡、Tilemap 和玩法环境进入对应资产生产路线。

| 背景 ID | 方向/场景 | 方案 | 基准尺寸 | 焦点安全区 | 裁切/延展 | 预算 | 适用 V2 与 V5 联合证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |

## 一致性验收

- [ ] V0 分流、适用 V 阶段和审核层均有记录。
- [ ] 适用 V1 时玩法视觉契约和早期预算完整；原子资源已记录既有适用有效契约与预算基线，缺少基线时已升级路径。
- [ ] 适用 V2 以及所有 V5 均有动态可玩片段或可复现交互轨迹。
- [ ] 完成结论仅要求适用 F2、适用时 F3 和按需 F4，原子资源未因 V5 自动进入 F3。
- [ ] 同条件截图记录 ROI、容差、时间采样/稳定帧与遮罩；生成式、动画和 VFX 未只用像素差判断。
- [ ] 正式资源均保留可编辑来源或生成记录，并有授权、运行时输出、Phaser 与玩法视觉证据。
- [ ] V5 已完成结构化集成和低保真清理。
""",
    "TDD.md": """# 技术设计文档

## 技术基线

- 游戏：Phaser 4 + TypeScript + Vite
- 原生容器：Capacitor
- 渠道适配：小游戏、iOS、Google Play 分别评估

## 模块边界与所有权

下表是模块清单索引；候选模块明细、依赖矩阵和确认记录是实现前强制门禁，三者必须使用相同模块 ID。

玩法独占规则、状态和交互代码。美术可拥有纯表现层资源配置、布局/表现预制数据和视觉集成调整，但不得改变玩法规则；V5 由玩法与美术协作。

| 模块 | 职责/非目标 | 输入/输出 | 状态所有权 | 依赖 | 生命周期/失败边界 | 测试 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

新模块首次实现或模块边界变化必须先完成 F0-F3、模块拷问和人工批准。以下信息不得以文件夹名或类名代替；只有确认记录完整且人工结论为“批准”时，模块状态才能标为“已批准待实现”。

### 候选模块明细

| 模块 ID/名称 | 职责/非目标 | 公开入口 | 公开输入/输出 | 状态/数据所有权 | 生命周期与失败/降级边界 | 资源/平台依赖 | 允许依赖 | 禁止依赖 | 测试边界 | 负责人 | 实现顺序 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

### 模块依赖矩阵

矩阵单元格填写公开契约 ID，或填写“禁止”“无依赖”；不得只写文件路径。循环依赖、双向状态写入和绕过公开入口的访问必须在批准前消除。

| 调用方 \\ 被调用方 | 模块 A | 模块 B | 平台/资源适配层 |
| --- | --- | --- | --- |
| 模块 A | 无依赖 | 待确认 | 待确认 |
| 模块 B | 待确认 | 无依赖 | 待确认 |
| 平台/资源适配层 | 禁止 | 禁止 | 无依赖 |

### 模块确认记录

确认记录必须绑定候选版本与实际模块；沉默、功能范围批准或旧模块批准均不构成本次边界批准。

| 确认 ID/候选版本 | 模块 ID | F0 入口 | F1 契约/依赖证据 | F2 非作者架构结论 | F3 跨模块结论 | 模块拷问/F4 记录 | 人工结论 | 确认日期/负责人 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 游戏实现增量

| 增量 | 玩家行为与验收 | 代码入口 | 资源依赖 | 测试 | 状态 |
| --- | --- | --- | --- | --- | --- |

## 视觉生产与资源接入契约

| 对象/状态 | V0 类型 | 适用阶段 | 玩法视觉契约/预算基线 | 路线 | 低保真 ID | 资源 ID/纹理键 | 正式路径 | V4 验收 | V5 集成/清理 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## UI 布局契约

UI 的 `x/y` 只表示相对已声明参照物的本地距离；资源原点不得替代布局关系。

| UI ID/状态 | 父级/坐标系 | 参照物 | 停靠点 | 距离/单位 | 尺寸策略 | 布局组 | 安全区/断点 | 误差阈值 | 实现入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 视觉运行时预算

| 场景 | 纹理/图集 | 帧数/采样率 | 纹理内存 | 包体 | 最大纹理 | 过绘 | Draw Call | 清单位置 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 构建与本地验证服务

启动前按项目根目录、服务类型、模式、端口、监听进程和健康状态查重；可复用时不得新启动。

| 服务 | 项目根目录 | 命令 | 主机/端口 | PID/会话 | 健康检查 | 复用/新启动 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
""",
    "control-plane.md": """# 项目控制面

本文件由总控独占写入。其他角色只提交状态包。

## 当前质量门与审核漏斗

| 审核 ID | 候选物/版本 | G 门/V 阶段 | 当前 F 层 | 作者 | 非作者审阅者 | 状态 | 阻断项 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

## F4 人工决策

| 日期 | 审核 ID/版本 | 受保护决策 | F0-F3 已关闭问题 | 推荐项 | 人工结论 | 下一步 |
| --- | --- | --- | --- | --- | --- | --- |

## 视觉完成审计

| 审计问题 | 结论 | 证据位置 |
| --- | --- | --- |
| V0 分流及适用 V/F 路径是什么？ | 待审计 |  |
| 本任务产出的 V1 玩法视觉契约/预算，或原子资源引用的既有基线在哪里？ | 待审计 |  |
| 适用 V2 与所有 V5 的动态可玩证据在哪里？ | 待审计 |  |
| V3 资产路线、可编辑来源与机器清单在哪里？ | 待审计 |  |
| V4 资源级验收和非作者 F2 在哪里？ | 待审计 |  |
| V5 集成 F1、动态证据、适用时跨域 F3 与低保真清理在哪里？ | 待审计 |  |
""",
}


ASSET_LICENSE_TEMPLATE = """# 资源与授权登记

本文件由美术接入角色维护；玩法独占规则、状态和交互代码。

初始化生成的 `visual-assets.json` 预算值均为 `null`，预期暂不通过预算校验；项目在 V1 冻结预算，或原子资源确认既有适用有效预算基线后，必须填写正数再执行正式验证。

| 资源 ID | 纹理键 | V0 类型 | 资产路线 | 状态 | 可编辑源文件/生成记录 | 来源与授权 | 运行时输出 | Phaser 证据 | 玩法视觉证据 | 适用审核结论 | 发布可用 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 生产包

| 交付包 | V 阶段 | 机器清单 | 玩法视觉契约/预算基线 | 未完成/未验收项 | 非作者审阅者 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |

AI 合成栅格的框选拆图仅是可选资产路线；UI、像素美术、动画、Tilemap、VFX 等按各自源文件和运行时输出验收。
"""


VISUAL_ASSETS_TEMPLATE = {
    "schema_version": "1.0",
    "budgets": {
        "max_texture_size": None,
        "texture_memory_mb": None,
        "package_size_mb": None,
        "max_atlases": None,
        "max_frames": None,
        "animation_sample_fps": None,
        "max_overdraw": None,
        "max_draw_calls": None,
    },
    "assets": [],
}


OPTIONAL_TEMPLATES = {
    "balance": {
        "balance.md": """# 数值设计

## 设计目标与玩家节奏

待数值设计角色补充。

## 参数与公式

| 参数 | 含义 | 默认值 | 来源 | 调整影响 | 验证方式 |
| --- | --- | --- | --- | --- | --- |

## 模拟与实测记录

待补充。
""",
    },
    "assets": {
        "asset-license-register.md": ASSET_LICENSE_TEMPLATE,
        "visual-assets.json": json.dumps(VISUAL_ASSETS_TEMPLATE, ensure_ascii=False, indent=2) + "\n",
    },
    "qa": {
        "qa-plan.md": """# 测试与性能计划

## 缺陷分级与关闭

P0-P3 只采用 `phaser4-game-orchestrator/references/review-funnel.md` 的“P0-P3 缺陷优先级唯一标准”，本计划不复制定义。G2 前按该标准复验 P0/P1；P0/P1 豁免必须绑定当前候选版本并取得 F4 人工结论。

| 缺陷 ID | 候选版本 | P0-P3 | 判定证据 | 影响范围 | 修复/补偿措施 | 回归证据 | F4 豁免记录 | 负责人 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## 审核漏斗

F2 必须由非作者执行；缺少第二角色时由总控代审并标识。组件/资源集和场景/系统路径在 V5 做 F3；原子资源在 V5 做集成 F1 与动态证据，只有跨域风险才做 F3。

| 审核 ID/版本 | G 门/V 阶段 | F1 证据 | F2 非作者结论 | F3 综合结论 | 按需 F4 | 回归范围 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |

## 动态玩法视觉验证

适用 V2 以及所有 V5 必须提供动态可玩片段或可复现交互轨迹，验证识别、预警、反馈、遮挡与移动端缩小可读性。

| 契约 ID/状态 | 操作轨迹 | 第一视觉目标 | 预警/反馈时序 | 遮挡 | 小屏/高速/灰阶/色觉 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |

## 视觉对比记录

| 状态 | 同条件参数 | ROI | 容差 | 动画时间采样/稳定帧 | 遮罩及理由 | 并排/叠加/像素差 | 非像素质量结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |

生成式、动画和 VFX 不得只靠像素差通过。源码、构建成功和元素存在只属于结构或工程证据。

## 设备与性能矩阵

| 设备/视口 | 帧率 | 纹理内存 | 包体 | 最大纹理 | 过绘 | Draw Call | 预算结论 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
""",
    },
    "platform": {
        "platform-matrix.md": """# 平台矩阵

| 渠道 | 优先级 | 打包方式 | 必要适配 | 商店/平台资料 | 合规状态 | 负责人 |
| --- | --- | --- | --- | --- | --- | --- |
| 小游戏 | 待人工指定 | 待评估 | 待评估 | 待评估 | 待评估 | 发布合规 |
| iOS | 待人工指定 | Capacitor | 待评估 | 待评估 | 待评估 | 发布合规 |
| Google Play | 待人工指定 | Capacitor | 待评估 | 待评估 | 待评估 | 发布合规 |
| 其他 | 扩展 | 待评估 | 待评估 | 待评估 | 待评估 | 发布合规 |
""",
    },
    "release": {
        "release-checklist.md": """# 发布检查清单

## 发布候选版本

- 版本号：
- 构建提交：
- 构建命令：
- 候选包位置：

## 必须核验

- [ ] 人工已批准 G3。
- [ ] 每个目标渠道都有对应候选包和测试证据。
- [ ] 商店资料、隐私声明、分级和资源授权已核验。
- [ ] 没有密钥或商店凭据进入仓库。
- [ ] 已知风险、回滚或热修复方案已记录。
""",
    },
}


def parse_include_list(value: str) -> list[str]:
    """解析可选交付物名称，并拒绝未知名称。"""
    names = [name.strip() for name in value.split(",") if name.strip()]
    unknown = sorted(set(names) - OPTIONAL_TEMPLATES.keys())
    if unknown:
        valid = "、".join(OPTIONAL_TEMPLATES)
        raise argparse.ArgumentTypeError(
            f"不支持的交付物：{'、'.join(unknown)}。可选值：{valid}。"
        )
    return list(dict.fromkeys(names))


def parse_args() -> argparse.Namespace:
    """解析项目目录、可选交付物和覆盖授权。"""
    parser = argparse.ArgumentParser(description="按阶段初始化 Phaser 4 游戏协作文档")
    parser.add_argument("--project-root", required=True, type=Path, help="目标游戏项目根目录")
    parser.add_argument(
        "--include",
        type=parse_include_list,
        help="逗号分隔的可选交付物：balance、assets、qa、platform、release",
    )
    parser.add_argument("--force", action="store_true", help="明确覆盖本次选择的同名文档")
    return parser.parse_args()


def validate_project_root(project_root: Path) -> Path:
    """拒绝文件系统根目录，降低误写入过宽目录的风险。"""
    resolved = project_root.resolve()
    if resolved == resolved.parent:
        raise ValueError("项目目录不能是文件系统根目录。")
    return resolved


def select_templates(include: list[str] | None) -> dict[str, str]:
    """选择核心模板，或合并每个 include 对应的多个文件。"""
    if include is None:
        return dict(CORE_TEMPLATES)

    selected: dict[str, str] = {}
    for name in include:
        # 一个 include 可以展开为多个文件；先完整收集目标，才能原子地执行覆盖检查。
        selected.update(OPTIONAL_TEMPLATES[name])
    return selected


def initialize_documents(
    project_root: Path, templates: dict[str, str], force: bool
) -> list[Path]:
    """创建所选交付物，并在写入前统一检查覆盖冲突。"""
    docs_dir = project_root / "docs"
    targets = [docs_dir / filename for filename in templates]
    existing = [path for path in targets if path.exists()]
    if existing and not force:
        names = "、".join(path.name for path in existing)
        raise FileExistsError(f"拒绝覆盖已有文档：{names}。如确需覆盖，请显式传入 --force。")

    docs_dir.mkdir(parents=True, exist_ok=True)
    for path in targets:
        path.write_text(templates[path.name], encoding="utf-8")
    return targets


def main() -> int:
    """执行初始化并输出可审计的写入结果。"""
    args = parse_args()
    try:
        project_root = validate_project_root(args.project_root)
        templates = select_templates(args.include)
        written = initialize_documents(project_root, templates, args.force)
    except (OSError, ValueError) as error:
        print(f"初始化失败：{error}", file=sys.stderr)
        return 1

    print("已初始化项目交付物：")
    for path in written:
        print(path)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
