# 模块划分、审计与决策边界

新建模块、首次实现模块，或改动职责、公开契约、状态所有权、依赖方向、持久化或平台边界时读取本文件。模块划分不是按文件夹或类名分组，而是冻结可独立说明、实现、测试和替换的职责边界。

## 划分模块

1. 从已批准 GDD、TDD 和代码事实提取玩家行为、状态变化、平台能力、资源依赖、失败路径和验收证据；优先扩展边界清晰的现有模块。
2. 建立候选模块图，记录职责与非目标、公开入口和输入/输出、状态/数据所有权、生命周期、错误与降级、资源/平台依赖、允许/禁止依赖、测试边界和负责人。
3. 规定单向依赖和调用契约。共享能力只有被两个以上已确认模块稳定复用时才提取；消除或明确处理循环依赖、双向状态写入、无边界 `common`/`utils`、全局事件和绕过公开入口的访问。
4. 给出可独立验证的实现顺序：契约与纯规则、适配层和表现、跨模块集成。默认单写者顺序执行；用户明确要求并行写入时才拆成互斥写范围。

## 审计与审核

模块候选按统一 [审核漏斗](review-funnel.md) 执行：作者实际完成 F0；总控在 F1 按风险和变化分诊；模块/架构变化按风险触发独立技术 F2，行为或验收变化触发 QA F2；总控在 F3 收敛同一候选。新模块或边界变化本身不自动触发 grilling/F4，也不得仅因进入新阶段重复提问。

在生产实现前，先用代码、配置、权威工件和确定性执行关闭事实。若实质范围、风险、验收、依赖或不可逆架构选择阻断候选模块图形成，可在候选前 grilling 并绑定决策 ID/工件版本；已有候选时，候选相关 F4 必须在 F3 后。没有未决用户决定时直接继续。

## 实现与变更

1. 在 `docs/TDD.md` 记录已确认模块清单、依赖矩阵、候选身份和审核结论；如发生 F4，再链接人工决定。不得把沉默、功能范围批准或旧候选批准扩张为当前架构决定。
2. 实现发现边界变化时停止直接受影响范围，更新模块图，产生新候选并重跑受影响 F0/F1/F2/F3；只有仍有受保护取舍时再次进入 F4。模块内部、私有实现且不改变批准边界的细节无需 grilling。
3. 逐模块验证公开契约、单一状态写入所有者、禁止依赖、错误边界和可复现集成证据。模块只有边界与实现一致、测试通过、下游使用公开入口且无未批准依赖时才可标为“已实现”。
